Flat by default, folders when there's more than one.

Entity, repository, controller, and service files live at the module root. When a module has multiple entities or repositories, group them into `entities/` and `repositories/` folders. `use-cases/` and `dto/` always get their own folders.
